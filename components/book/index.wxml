<view bind:tap="onTab" class="container">
    <image src="{{Book.image}}"></image>
    <view class="description">
        <text class="tittle">{{Book.title}}</text>
        <text class="author">{{Book.author}}</text>
        <view class="foot">
            <text class="footer">{{Book.fav_nums}}</text>
        </view>
    </view>
</view>